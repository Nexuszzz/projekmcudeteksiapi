# MQTT Broker Configuration - CONFIGURED
# Rename this file to .env to use

# IMPORTANT: Broker port 1883 is TCP only (not WebSocket)
# Choose ONE of these options:

# ===== OPTION 1: Use Proxy Server (RECOMMENDED) =====
# Start proxy server first: cd proxy-server && npm install && npm start
# Then use this URL:
VITE_MQTT_URL=ws://localhost:8080/ws

# ===== OPTION 2: If broker has WebSocket support =====
# Try common WebSocket ports: 9001, 8083, or 8080
# Uncomment and test:
# VITE_MQTT_URL=ws://3.27.11.106:9001/mqtt

# MQTT Authentication
VITE_MQTT_USERNAME=zaks
VITE_MQTT_PASSWORD=enggangodinginmcu

# MQTT Topics - lab/zaks/#
VITE_TOPIC_EVENT=lab/zaks/event
VITE_TOPIC_LOG=lab/zaks/log
VITE_TOPIC_STATUS=lab/zaks/status
VITE_TOPIC_ALERT=lab/zaks/alert
# Command topic (ESP32 subscribe)
VITE_TOPIC_CMD=nimak/deteksi-api/cmd

# Optional: Data retention
VITE_MAX_DATA_POINTS=10000
